const colors = require('colors');
// const colors = require('./colors');

//maneras de usar require -> Core node (mòdulos) 
//maneras de usar require -> archivos módulos (exportaciones) -> creadas por nosotros
//maneras de usar require -> paquetes instalados por npm 

console.log('hola mundo' . gray);